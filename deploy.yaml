trigger: none
pr: none

pool:
  vmImage: windows-2019

resources:
  pipelines:
  - pipeline: test
    source: test
    # branch: refs/heads/develop

jobs:
  - job: TestJob
    displayName: Test Job
    steps:
    - download: test

    - pwsh: |
        Get-ChildItem
      workingDirectory: $(Pipeline.Workspace)/test/test
      displayName: pwsh test