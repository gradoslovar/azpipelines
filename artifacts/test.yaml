trigger:
  branches:
    include:
      - develop
      - main
      - refs/tags/v*
  paths:
    include:
      - infra/base.tf
      - infra/base.tfvars

pr: none

name: $(BuildID)

pool:
  vmImage: windows-2019

steps:

- task: CopyFiles@2
  displayName: Copy afad-base module
  inputs:
    SourceFolder: infra
    Contents: |
     **/**.tf
    TargetFolder: $(Build.ArtifactStagingDirectory)/infra

- publish: '$(Build.ArtifactStagingDirectory)'
  artifact: base-dev
  displayName: Publish Artifacts