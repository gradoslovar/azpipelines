trigger:
  branches:
    include:
      - refs/tags/v*

pr: none

name: $(BuildID)

pool:
  # vmImage: ubuntu-latest
  vmImage: windows-2019

stages:
  - stage: Stage1
    dependsOn: []
    # pool:
    #   vmImage: ubuntu-latest
    jobs:
    - job: Stage1
      steps:
      - pwsh: |
          Write-Host "Stage11"

      - template: ../templates/template.yaml

  - stage: Stage2
    dependsOn: []
    # pool:
    #   vmImage: ubuntu-latest
    jobs:
    - job: Stage2
      steps:
      - pwsh: |
          Write-Host "Stage2"

      - template: ../templates/template.yaml